<VirtualHost *:80>
    ServerAdmin webmaster@localhost
    DocumentRoot /var/www/MakeAtState/web
    ErrorLog ${APACHE_LOG_DIR}/error.log
    CustomLog ${APACHE_LOG_DIR}/access.log combined

    RewriteEngine on
    #RewriteRule ^/(.*) https://%{HTTP_HOST}/$1 [R,L]
</VirtualHost>

